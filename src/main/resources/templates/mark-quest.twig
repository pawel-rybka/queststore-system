<!DOCTYPE html>
<html lang="en">
<script src="../static/lib/w3.js"></script>
    
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <link rel="stylesheet" href="../static/css/style.css">
</head>
    
<body>
    <div class="wrapper">
        
        <div class="menu" w3-include-html="../static/mentor/mentor-menu.html"></div>

        <div class="content">
            <p class="log-bar">You are logged as Mentor</p>
            <h2>Choose quest to mark:</h2>
            <h3>quest - student</h3>
            <form>
                {%for completedQuest in completedQuests%}
                <input type="radio" name="completedQuest" value="{{ completedQuest.getId }}">
                    {{ qDao.getQuestById(completedQuest.getQuestId).getName }} -
                    {{ sDao.getStudentById(completedQuest.getStudentId).getFullName() }} <br><br>
                {%endfor%}
                <button type="submit" formmethod="post">Mark chosen quest</button>
            </form>
            
        </div>
    </div>
    
    <script>
        w3.includeHTML();
    </script>
    
</body>
    
</html>