<!DOCTYPE html>
<html lang="en">
<script src="../static/lib/w3.js"></script>
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <link rel="stylesheet" href="../static/css/style.css">
</head>
<body>
    <div class="wrapper">
        <div class="menu" w3-include-html="../static/admin-menu.html"></div>

        <div class="content">
            <p class="log-bar">You are logged as Admin</p>
            <h2>Mentor's data:</h2>
            <form>
                <input type="hidden" name="mentor" value="{{ mentor.getId }}"/>

                <div class="two-columns">
                    <div class="form-labels">
                        <p>First name:</p>
                        <p>Last name:</p>
                        <p>Phone number:</p>
                        <p>Email:</p>
                        <p>Password:</p>
                        <p>Class:</p>
                    </div>

                    <div class="form-values">
                        <p>{{ mentor.getFirstName }}</p>
                        <p>{{ mentor.getLastName }}</p>
                        <p>{{ mentor.getPhoneNumber }}</p>
                        <p>{{ mentor.getEmail }}</p>
                        <p>{{ mentor.getPassword }}</p>
                        <p>{{ klass.getClassName }}</p>
                    </div>
                </div>
                <button type="submit" formaction="edit-mentor" formmethod="post">Edit</button>
                <button type="submit" formaction="remove-mentor" formmethod="post">Delete</button>
            </form>
        </div>
    </div>
    <script>
        w3.includeHTML();
    </script>

</body>
</html>